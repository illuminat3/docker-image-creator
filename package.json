{
  "name": "docker-image-creator",
  "version": "1.0.0",
  "description": "Build and push Docker images from git repos, bypassing Cloudflare upload limits",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
